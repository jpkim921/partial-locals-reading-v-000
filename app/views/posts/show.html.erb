Information About the Post
<!-- because of the line below, we can delete @author = @post.author in posts_controller -->
<%= render partial: "authors/author", locals: {post_author: @author} %>

<%= @post.title %>
<%= @post.content %>
